
@{
    ViewBag.Title = "Admin";
    var currentController = ViewContext.RouteData.Values["Controller"]?.ToString();
    var currentAction = ViewContext.RouteData.Values["Action"]?.ToString();
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - Quản lý xe khách</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="~/Content/css/ThongKe.css">
</head>
<body>
    <!-- SIDEBAR -->
    <aside class="sidebar">
        <div class="sidebar-header">
            <h2><i class="fas fa-bus"></i> Quản lý xe khách</h2>
        </div>
        <ul class="nav-menu">
            @*<li class="nav-item">
            <a href="#dashboard" class="nav-link active">
                <i class="fas fa-chart-line"></i>
                <span>Dashboard</span>
            </a>
        </li>
        <li class="nav-item">
            <a href="#routes" class="nav-link">
                <i class="fas fa-route"></i>
                <span>Quản lý lộ trình</span>
            </a>
        </li>
        <li class="nav-item">
            <a href="#bookings" class="nav-link">
                <i class="fas fa-ticket-alt"></i>
                <span>Đặt vé</span>
            </a>
        </li>
        <li class="nav-item">
            <a href="#drivers" class="nav-link">
                <i class="fas fa-id-card"></i>
                <span>Quản lý tài xế</span>
            </a>
        </li>
        <li class="nav-item">
            <a href="#staff" class="nav-link">
                <i class="fas fa-users"></i>
                <span>Nhân viên quầy vé</span>
            </a>
        </li>
        <li class="nav-item">
            <a href="#customers" class="nav-link">
                <i class="fas fa-user-friends"></i>
                <span>Khách hàng</span>
            </a>
        </li>*@
            @*<li class="nav-item">
            <a href="#statistics" class="nav-link">
                <i class="fas fa-chart-bar"></i>
                <span>Thống kê</span>
            </a>
        </li>*@
            <li class="nav-item">
                <a href="@Url.Action("ThongKe", "Admin")"
                   class="nav-link @(currentAction=="ThongKe" ? "active" : "")">
                    <i class="fas fa-chart-bar"></i>
                    <span>Thống kê</span>
                </a>
            </li>

            <li class="nav-item">
                <a href="@Url.Action("KhachHangThanThiet", "Admin")"
                   class="nav-link @(currentAction=="KhachHangThanThiet" ? "active" : "")">
                    <i class="fas fa-user-friends"></i>
                    <span>Khách hàng thân thiết</span>
                </a>
            </li>
            <li class="nav-item">
                <a href="@Url.Action("Index", "Trip")"
                   class="nav-link @(currentAction=="Index" ? "active" : "")">
                    <i class="fas fa-user-friends"></i>
                    <span>Quản lý chuyến đi</span>
                </a>
            </li>


            @*<li class="nav-item">
            <a href="#settings" class="nav-link">
                <i class="fas fa-cog"></i>
                <span>Cài đặt</span>
            </a>
        </li>*@
        </ul>
    </aside>

    <!-- HEADER -->
    <header class="header">
        <div class="header-left">
            <h1>Dashboard</h1>
        </div>
        <div class="header-right">
            <div class="user-info">
                <div class="user-avatar">A</div>
                <form method="post" action="@Url.Action("Logout", "Auth")" class="d-inline ms-auto">
                    @Html.AntiForgeryToken()
                    <button type="submit" class="btn btn-outline-light btn-logout">
                        <i class="bi bi-box-arrow-right me-2"></i>
                        <span class="d-none d-sm-inline">Đăng xuất</span>
                    </button>
                </form>
            </div>
        </div>
    </header>

    <!-- MAIN CONTENT -->
    <main class="main-content">
        @RenderBody()
    </main>

</body>
</html>

